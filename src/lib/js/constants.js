// be carefull with seperation space, check background generation works
export const pipeSeperationSpace = 650;
export const extraFirstPipeSpace = 1000;

export const pipeWidth = 98;
export const birdWidth = 70;
export const birdHeight = 50;
export const openingHeight = 280;
export const horizontalVelocity = 2.5;
export const jumpVelocity = -9; // velocity is pointed downwards, so negative velocity is pointer up
export const gravity = 0.25; // gravitational accelaration
export const offset = 7 // offset is how much leaniancy the player gets, so the bird has to be x amount of pixels into a pipe for it to register
export const yStart = 400
export const xStart = 500

